
# pick out forecast based on desired poll [feature]
# load the functions
# load the raw data
# apply the function to the raw data with the correct settings (inputs)
# return the results as a vector, or table?
